<h1><%= _("Editing language") %></h1>

<%= error_messages_for :language %>

<% form_for(@language) do |f| %>
  <p>
    <b><%= _("Name") %></b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit _("Update") %>
  </p>
<% end %>

<%= link_to 'Show', @language %> |
<%= link_to 'Back', languages_path %>
