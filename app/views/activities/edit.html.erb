<h1><%= _("Editing activity") %></h1>

<%= error_messages_for :activity %>

<% form_for(@activity) do |f| %>
  <p>
    <b><%= _("Person") %></b><br />
    <%= f.text_field :person_id %>
  </p>

  <p>
    <b><%= _("Instance") %></b><br />
    <%= f.text_field :item_id %>
  </p>

  <p>
    <b><%= _("Type") %></b><br />
    <%= f.text_field :item_type %>
  </p>

  <p>
    <%= f.submit _("Update") %>
  </p>
<% end %>

<%= link_to 'Show', @activity %> |
<%= link_to 'Back', activities_path %>
