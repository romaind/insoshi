<div class="accordion_toggle tab_title clearfix hand"><h4>Project data</h4></div>
<div class="accordion_content content">

	<h4><%= _("Created at: ") %></h4>
  <p>
  	<%=h @project.created_at.strftime(_("%d/%m/%Y")) %>
  	<%=h @project.created_at.strftime(_(" at %I:%M%p")) %>
  </p>

  	<h4><%= _("Author: ") %></h4>
  <p>
  	<%=h @project.person.first_name + " " + @project.person.name %>
  </p>

  	<h4><%= _("Categories: ") %></h4>
  <p>
  	<%- if @project.skills %>
  		<%= @project.skills.collect{|skill| skill.name}.join(", ") %>
  	<%- end %>
  </p>

  	<h4><%= _("Tags: ") %></h4>
  <p>
  	<%- if @project.tags.first %>
      	<%= @project.tags.collect{|t| t.name}.join(", ") %>
  	<%- end %>
  </p>

  <h4><%= _("Licence:") %></h4>
  	<%= cc_icons(@project.common) %>
</div>