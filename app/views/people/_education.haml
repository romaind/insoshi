#contact_content

-form_for @person, :html => { :multipart => true } do |f|

  #skills_content
    %h3 Skills and Education
    
    .form_row
      / %div
      /   -@person.people_schools.each do |sc|
      /     %label Existing schools:
      /     %input{ :name => "school[][name]", :size => "30", :type => "text", :value => sc.school.name}
      /     -i = Date.today.year+5
      /     %select{:id => "school_year", :name => "school[][year]"}
      /       -25.times do
      /         -if i==sc.year
      /           %option{:value => i, :selected => "selected"}
      /             =i
      /         -else
      /           %option{:value => i}
      /             =i
      /         -i = i-1
      #schools
        =render :partial => "school", :collection => @person.schools
      =link_to_function "Add a school" do |page|
        -page.insert_html :bottom, :schools, :partial => "school", :object => School.new

    #languages_checkboxes
      %h3 Languages
      =render :partial => "languages", :locals =>{ :f => f }
  
    #softwares_checkboxes
      %h3 Software
      =render :partial => "software", :locals =>{ :f => f }

  .form_row
    #cv
      %h3 CV
      %label{ :for => "person_cv" } CV (PDF format):
      =f.file_field :cv

  %input{ :type => "hidden", :name => "type", :value => "skill_edit"}

  = f.submit "Update", :class => "button"
